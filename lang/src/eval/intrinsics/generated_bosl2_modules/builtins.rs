// GENERATED by `fab_lib::emit::generate_standalone_modules` (AR.14.4 bands 1+2) — DO NOT EDIT.
// Refresh: FAB_REGEN=1 cargo nextest run -p fab-lib -E 'test(generated_bosl2_modules_are_current)'
//
// Natives for BOSL2 `builtins.scad` — one generated file per library SOURCE file (AR.23), so
// a regen diff stays local to the .scad that moved. The registry naming these lives in
// `mod.rs`.
//
// A module native builds GEOMETRY through the interpreter's own construction (`ModuleCtx`),
// so what it renders is what interpreting its reference renders — the win is the deleted
// interpretation overhead, not different geometry.

// rustfmt CANNOT format this file: the emitter's one-line expression bodies blow up its
// layout search (measured: 5 CPU-minutes without terminating at 402 modules), and the
// bytes are regenerated verbatim anyway — reformatting would only fail the currency gate.
#![cfg_attr(rustfmt, rustfmt::skip)]
#![allow(
    unused_variables,
    unused_mut,
    non_snake_case,
    clippy::get_first,
    clippy::vec_init_then_push,
    clippy::unreadable_literal,
    clippy::cloned_ref_to_slice_refs,
    clippy::used_underscore_items,
    clippy::possible_missing_else,
    clippy::collapsible_else_if,
    clippy::similar_names,
    clippy::needless_else,
    clippy::if_same_then_else,
    clippy::too_many_lines,
    reason = "generated code: a module need not READ every parameter it declares, \
              parameter slots are indexed uniformly (so slot 0 is `get(0)`, not \
              `first()`), and a parts vec grows CONDITIONALLY even when the first \
              push happens to be unconditional — plus bit-exact from_bits literals, \
              mechanical clones, upstream's underscore-prefixed and camelCase names \
              carried verbatim, fresh idents that differ by counter (`l1_r`/`l2_r`), \
              upstream's own empty/identical branches, and bodies as long as the \
              module they transcribe"
)]

// AR.13: `rt` is the ONLY thing generated code names.
use fab_lang::rt;

/// Generated native for module `_circle` — geometry through the interpreter's own
/// construction, so a generated module is what interpreting its reference builds.
pub(super) fn _circle(fx: &dyn rt::ModuleCtx) -> rt::Result<rt::Geo> {
    let p_r = fx.args().get(0).cloned().unwrap_or(rt::Value::Undef);
    let p_d = fx.args().get(1).cloned().unwrap_or(rt::Value::Undef);
    let mut parts: Vec<rt::Geo> = Vec::new();
    parts.push(fx.call(&rt::ModuleCall { name: "circle", args: &[(Some("r"), p_r.clone()), (Some("d"), p_d.clone()), ], children: rt::Children::None })?);
    Ok(fx.group(parts))
}


/// Generated native for module `_color` — geometry through the interpreter's own
/// construction, so a generated module is what interpreting its reference builds.
pub(super) fn _color(fx: &dyn rt::ModuleCtx) -> rt::Result<rt::Geo> {
    let p_color = fx.args().get(0).cloned().unwrap_or(rt::Value::Undef);
    let mut parts: Vec<rt::Geo> = Vec::new();
    if (rt::Value::Bool(rt::apply_binary(rt::BinOp::Eq, p_color.clone(), rt::Value::Undef).is_truthy() || rt::apply_binary(rt::BinOp::Eq, p_color.clone(), rt::Value::string("default")).is_truthy())).is_truthy() {
    parts.push(fx.children()?);
    } else {
    parts.push(fx.call(&rt::ModuleCall { name: "color", args: &[(None, p_color.clone()), ], children: rt::Children::Compiled(&[&|fx: &dyn rt::ModuleCtx| { let mut parts: Vec<rt::Geo> = Vec::new();     parts.push(fx.children()?);
 Ok(fx.group(parts)) }, ]) })?);
    }
    Ok(fx.group(parts))
}


/// Generated native for module `_cube` — geometry through the interpreter's own
/// construction, so a generated module is what interpreting its reference builds.
pub(super) fn _cube(fx: &dyn rt::ModuleCtx) -> rt::Result<rt::Geo> {
    let p_size = fx.args().get(0).cloned().unwrap_or(rt::Value::Undef);
    let p_center = fx.args().get(1).cloned().unwrap_or(rt::Value::Undef);
    let mut parts: Vec<rt::Geo> = Vec::new();
    parts.push(fx.call(&rt::ModuleCall { name: "cube", args: &[(None, p_size.clone()), (Some("center"), p_center.clone()), ], children: rt::Children::None })?);
    Ok(fx.group(parts))
}


/// Generated native for module `_cylinder` — geometry through the interpreter's own
/// construction, so a generated module is what interpreting its reference builds.
pub(super) fn _cylinder(fx: &dyn rt::ModuleCtx) -> rt::Result<rt::Geo> {
    let p_h = fx.args().get(0).cloned().unwrap_or(rt::Value::Undef);
    let p_r1 = fx.args().get(1).cloned().unwrap_or(rt::Value::Undef);
    let p_r2 = fx.args().get(2).cloned().unwrap_or(rt::Value::Undef);
    let p_center = fx.args().get(3).cloned().unwrap_or(rt::Value::Undef);
    let p_r = fx.args().get(4).cloned().unwrap_or(rt::Value::Undef);
    let p_d = fx.args().get(5).cloned().unwrap_or(rt::Value::Undef);
    let p_d1 = fx.args().get(6).cloned().unwrap_or(rt::Value::Undef);
    let p_d2 = fx.args().get(7).cloned().unwrap_or(rt::Value::Undef);
    let mut parts: Vec<rt::Geo> = Vec::new();
    parts.push(fx.call(&rt::ModuleCall { name: "cylinder", args: &[(None, p_h.clone()), (Some("r"), p_r.clone()), (Some("d"), p_d.clone()), (Some("r1"), p_r1.clone()), (Some("r2"), p_r2.clone()), (Some("d1"), p_d1.clone()), (Some("d2"), p_d2.clone()), (Some("center"), p_center.clone()), ], children: rt::Children::None })?);
    Ok(fx.group(parts))
}


/// Generated native for module `_multmatrix` — geometry through the interpreter's own
/// construction, so a generated module is what interpreting its reference builds.
pub(super) fn _multmatrix(fx: &dyn rt::ModuleCtx) -> rt::Result<rt::Geo> {
    let p_m = fx.args().get(0).cloned().unwrap_or(rt::Value::Undef);
    let mut parts: Vec<rt::Geo> = Vec::new();
    parts.push(fx.call(&rt::ModuleCall { name: "multmatrix", args: &[(None, p_m.clone()), ], children: rt::Children::Compiled(&[&|fx: &dyn rt::ModuleCtx| { let mut parts: Vec<rt::Geo> = Vec::new();     parts.push(fx.children()?);
 Ok(fx.group(parts)) }, ]) })?);
    Ok(fx.group(parts))
}


/// Generated native for module `_rotate` — geometry through the interpreter's own
/// construction, so a generated module is what interpreting its reference builds.
pub(super) fn _rotate(fx: &dyn rt::ModuleCtx) -> rt::Result<rt::Geo> {
    let p_a = fx.args().get(0).cloned().unwrap_or(rt::Value::Undef);
    let p_v = fx.args().get(1).cloned().unwrap_or(rt::Value::Undef);
    let mut parts: Vec<rt::Geo> = Vec::new();
    parts.push(fx.call(&rt::ModuleCall { name: "rotate", args: &[(Some("a"), p_a.clone()), (Some("v"), p_v.clone()), ], children: rt::Children::Compiled(&[&|fx: &dyn rt::ModuleCtx| { let mut parts: Vec<rt::Geo> = Vec::new();     parts.push(fx.children()?);
 Ok(fx.group(parts)) }, ]) })?);
    Ok(fx.group(parts))
}


/// Generated native for module `_scale` — geometry through the interpreter's own
/// construction, so a generated module is what interpreting its reference builds.
pub(super) fn _scale(fx: &dyn rt::ModuleCtx) -> rt::Result<rt::Geo> {
    let p_v = fx.args().get(0).cloned().unwrap_or(rt::Value::Undef);
    let mut parts: Vec<rt::Geo> = Vec::new();
    parts.push(fx.call(&rt::ModuleCall { name: "scale", args: &[(None, p_v.clone()), ], children: rt::Children::Compiled(&[&|fx: &dyn rt::ModuleCtx| { let mut parts: Vec<rt::Geo> = Vec::new();     parts.push(fx.children()?);
 Ok(fx.group(parts)) }, ]) })?);
    Ok(fx.group(parts))
}


/// Generated native for module `_sphere` — geometry through the interpreter's own
/// construction, so a generated module is what interpreting its reference builds.
pub(super) fn _sphere(fx: &dyn rt::ModuleCtx) -> rt::Result<rt::Geo> {
    let p_r = fx.args().get(0).cloned().unwrap_or(rt::Value::Undef);
    let p_d = fx.args().get(1).cloned().unwrap_or(rt::Value::Undef);
    let mut parts: Vec<rt::Geo> = Vec::new();
    parts.push(fx.call(&rt::ModuleCall { name: "sphere", args: &[(Some("r"), p_r.clone()), (Some("d"), p_d.clone()), ], children: rt::Children::None })?);
    Ok(fx.group(parts))
}


/// Generated native for module `_square` — geometry through the interpreter's own
/// construction, so a generated module is what interpreting its reference builds.
pub(super) fn _square(fx: &dyn rt::ModuleCtx) -> rt::Result<rt::Geo> {
    let p_size = fx.args().get(0).cloned().unwrap_or(rt::Value::Undef);
    let p_center = fx.args().get(1).cloned().unwrap_or(rt::Value::Undef);
    let mut parts: Vec<rt::Geo> = Vec::new();
    parts.push(fx.call(&rt::ModuleCall { name: "square", args: &[(None, p_size.clone()), (Some("center"), p_center.clone()), ], children: rt::Children::None })?);
    Ok(fx.group(parts))
}


/// Generated native for module `_text` — geometry through the interpreter's own
/// construction, so a generated module is what interpreting its reference builds.
pub(super) fn _text(fx: &dyn rt::ModuleCtx) -> rt::Result<rt::Geo> {
    let p_text = fx.args().get(0).cloned().unwrap_or(rt::Value::Undef);
    let p_size = fx.args().get(1).cloned().unwrap_or(rt::Value::Undef);
    let p_font = fx.args().get(2).cloned().unwrap_or(rt::Value::Undef);
    let p_halign = fx.args().get(3).cloned().unwrap_or(rt::Value::Undef);
    let p_valign = fx.args().get(4).cloned().unwrap_or(rt::Value::Undef);
    let p_spacing = fx.args().get(5).cloned().unwrap_or(rt::Value::Undef);
    let p_direction = fx.args().get(6).cloned().unwrap_or(rt::Value::Undef);
    let p_language = fx.args().get(7).cloned().unwrap_or(rt::Value::Undef);
    let p_script = fx.args().get(8).cloned().unwrap_or(rt::Value::Undef);
    let mut parts: Vec<rt::Geo> = Vec::new();
    parts.push(fx.call(&rt::ModuleCall { name: "text", args: &[(None, p_text.clone()), (Some("size"), p_size.clone()), (Some("font"), p_font.clone()), (Some("halign"), p_halign.clone()), (Some("valign"), p_valign.clone()), (Some("spacing"), p_spacing.clone()), (Some("direction"), p_direction.clone()), (Some("language"), p_language.clone()), (Some("script"), p_script.clone()), ], children: rt::Children::None })?);
    Ok(fx.group(parts))
}


/// Generated native for module `_translate` — geometry through the interpreter's own
/// construction, so a generated module is what interpreting its reference builds.
pub(super) fn _translate(fx: &dyn rt::ModuleCtx) -> rt::Result<rt::Geo> {
    let p_v = fx.args().get(0).cloned().unwrap_or(rt::Value::Undef);
    let mut parts: Vec<rt::Geo> = Vec::new();
    parts.push(fx.call(&rt::ModuleCall { name: "translate", args: &[(None, p_v.clone()), ], children: rt::Children::Compiled(&[&|fx: &dyn rt::ModuleCtx| { let mut parts: Vec<rt::Geo> = Vec::new();     parts.push(fx.children()?);
 Ok(fx.group(parts)) }, ]) })?);
    Ok(fx.group(parts))
}

