// GENERATED by `fab_lib::emit::generate_standalone_modules` (AR.14.4 bands 1+2) — DO NOT EDIT.
// Refresh: FAB_REGEN=1 cargo nextest run -p fab-lib -E 'test(generated_bosl2_modules_are_current)'
//
// Natives for BOSL2 `color.scad` — one generated file per library SOURCE file (AR.23), so
// a regen diff stays local to the .scad that moved. The registry naming these lives in
// `mod.rs`.
//
// A module native builds GEOMETRY through the interpreter's own construction (`ModuleCtx`),
// so what it renders is what interpreting its reference renders — the win is the deleted
// interpretation overhead, not different geometry.

// rustfmt CANNOT format this file: the emitter's one-line expression bodies blow up its
// layout search (measured: 5 CPU-minutes without terminating at 402 modules), and the
// bytes are regenerated verbatim anyway — reformatting would only fail the currency gate.
#![cfg_attr(rustfmt, rustfmt::skip)]
#![allow(
    unused_variables,
    unused_mut,
    non_snake_case,
    clippy::get_first,
    clippy::vec_init_then_push,
    clippy::unreadable_literal,
    clippy::cloned_ref_to_slice_refs,
    clippy::used_underscore_items,
    clippy::possible_missing_else,
    clippy::collapsible_else_if,
    clippy::similar_names,
    clippy::needless_else,
    clippy::if_same_then_else,
    clippy::too_many_lines,
    reason = "generated code: a module need not READ every parameter it declares, \
              parameter slots are indexed uniformly (so slot 0 is `get(0)`, not \
              `first()`), and a parts vec grows CONDITIONALLY even when the first \
              push happens to be unconditional — plus bit-exact from_bits literals, \
              mechanical clones, upstream's underscore-prefixed and camelCase names \
              carried verbatim, fresh idents that differ by counter (`l1_r`/`l2_r`), \
              upstream's own empty/identical branches, and bodies as long as the \
              module they transcribe"
)]

// AR.13: `rt` is the ONLY thing generated code names.
use fab_lang::rt;

/// Generated native for module `color_overlaps` — geometry through the interpreter's own
/// construction, so a generated module is what interpreting its reference builds.
pub(super) fn color_overlaps(fx: &dyn rt::ModuleCtx) -> rt::Result<rt::Geo> {
    let p_color = fx.args().get(0).cloned().unwrap_or(rt::Value::Undef);
    let mut parts: Vec<rt::Geo> = Vec::new();
    let l3_pairs = { let mut l0_acc: Vec<rt::Value> = Vec::new(); for l1_i in rt::iter_values_native(&rt::build_range(&rt::Value::Num(f64::from_bits(0x0_u64)), &rt::Value::Num(f64::from_bits(0x3ff0000000000000_u64)), &rt::apply_binary(rt::BinOp::Sub, fx.dollar("$children"), rt::Value::Num(f64::from_bits(0x3ff0000000000000_u64))))) { for l2_j in rt::iter_values_native(&rt::build_range(&rt::apply_binary(rt::BinOp::Add, l1_i.clone(), rt::Value::Num(f64::from_bits(0x3ff0000000000000_u64))), &rt::Value::Num(f64::from_bits(0x3ff0000000000000_u64)), &rt::apply_binary(rt::BinOp::Sub, fx.dollar("$children"), rt::Value::Num(f64::from_bits(0x3ff0000000000000_u64))))) { l0_acc.push(rt::build_vector(vec![l1_i.clone(), l2_j.clone()]));
        } } rt::build_vector(l0_acc) };
    let l4_blk = {
        let mut parts: Vec<rt::Geo> = Vec::new();
    for l5_p in rt::iter_values_native(&l3_pairs.clone()) {
    parts.push(fx.call(&rt::ModuleCall { name: "color", args: &[(None, p_color.clone()), ], children: rt::Children::Compiled(&[&|fx: &dyn rt::ModuleCtx| { let mut parts: Vec<rt::Geo> = Vec::new();     parts.push(fx.call(&rt::ModuleCall { name: "intersection", args: &[], children: rt::Children::Compiled(&[&|fx: &dyn rt::ModuleCtx| { let mut parts: Vec<rt::Geo> = Vec::new();     let l6_sel = rt::member(l5_p.clone(), "x");
    parts.push(fx.child_at(&l6_sel)?);
 Ok(fx.group(parts)) }, &|fx: &dyn rt::ModuleCtx| { let mut parts: Vec<rt::Geo> = Vec::new();     let l7_sel = rt::member(l5_p.clone(), "y");
    parts.push(fx.child_at(&l7_sel)?);
 Ok(fx.group(parts)) }, ]) })?);
 Ok(fx.group(parts)) }, ]) })?);
    }
    let l8_bg = parts.len();
    parts.push(fx.children()?);
    parts.truncate(l8_bg);
        fx.group(parts)
    };
    parts.push(l4_blk);
    Ok(fx.group(parts))
}


/// Generated native for module `color_this` — geometry through the interpreter's own
/// construction, so a generated module is what interpreting its reference builds.
pub(super) fn color_this(fx: &dyn rt::ModuleCtx) -> rt::Result<rt::Geo> {
    let p_c = fx.args().get(0).cloned().unwrap_or(rt::Value::Undef);
    let mut parts: Vec<rt::Geo> = Vec::new();
    fx.set_dollar("$save_color", fx.call_fn(&rt::FnCall { name: "default", args: &[(None, fx.dollar("$color")), (None, rt::Value::string("default"))] })?);
    fx.set_dollar("$color", p_c.clone());
    let l0_blk = {
        let mut parts: Vec<rt::Geo> = Vec::new();
    parts.push(fx.call(&rt::ModuleCall { name: "req_children", args: &[(None, fx.dollar("$children")), ], children: rt::Children::None })?);
    parts.push(fx.children()?);
        fx.group(parts)
    };
    parts.push(l0_blk);
    Ok(fx.group(parts))
}


/// Generated native for module `ghost` — geometry through the interpreter's own
/// construction, so a generated module is what interpreting its reference builds.
pub(super) fn ghost(fx: &dyn rt::ModuleCtx) -> rt::Result<rt::Geo> {
    let p_ghost = fx.args().get(0).cloned().unwrap_or(rt::Value::Undef);
    let mut parts: Vec<rt::Geo> = Vec::new();
    fx.set_dollar("$ghost", p_ghost.clone());
    let l0_blk = {
        let mut parts: Vec<rt::Geo> = Vec::new();
    parts.push(fx.children()?);
        fx.group(parts)
    };
    parts.push(l0_blk);
    Ok(fx.group(parts))
}


/// Generated native for module `ghost_this` — geometry through the interpreter's own
/// construction, so a generated module is what interpreting its reference builds.
pub(super) fn ghost_this(fx: &dyn rt::ModuleCtx) -> rt::Result<rt::Geo> {
    let mut parts: Vec<rt::Geo> = Vec::new();
    fx.set_dollar("$ghost_this", rt::Value::Bool(true));
    let l0_blk = {
        let mut parts: Vec<rt::Geo> = Vec::new();
    parts.push(fx.children()?);
        fx.group(parts)
    };
    parts.push(l0_blk);
    Ok(fx.group(parts))
}


/// Generated native for module `highlight` — geometry through the interpreter's own
/// construction, so a generated module is what interpreting its reference builds.
pub(super) fn highlight(fx: &dyn rt::ModuleCtx) -> rt::Result<rt::Geo> {
    let p_highlight = fx.args().get(0).cloned().unwrap_or(rt::Value::Undef);
    let mut parts: Vec<rt::Geo> = Vec::new();
    fx.set_dollar("$highlight", p_highlight.clone());
    let l0_blk = {
        let mut parts: Vec<rt::Geo> = Vec::new();
    parts.push(fx.children()?);
        fx.group(parts)
    };
    parts.push(l0_blk);
    Ok(fx.group(parts))
}


/// Generated native for module `highlight_this` — geometry through the interpreter's own
/// construction, so a generated module is what interpreting its reference builds.
pub(super) fn highlight_this(fx: &dyn rt::ModuleCtx) -> rt::Result<rt::Geo> {
    let mut parts: Vec<rt::Geo> = Vec::new();
    fx.set_dollar("$highlight_this", rt::Value::Bool(true));
    let l0_blk = {
        let mut parts: Vec<rt::Geo> = Vec::new();
    parts.push(fx.children()?);
        fx.group(parts)
    };
    parts.push(l0_blk);
    Ok(fx.group(parts))
}


/// Generated native for module `hsl` — geometry through the interpreter's own
/// construction, so a generated module is what interpreting its reference builds.
pub(super) fn hsl(fx: &dyn rt::ModuleCtx) -> rt::Result<rt::Geo> {
    let p_h = fx.args().get(0).cloned().unwrap_or(rt::Value::Undef);
    let p_s = fx.args().get(1).cloned().unwrap_or(rt::Value::Undef);
    let p_l = fx.args().get(2).cloned().unwrap_or(rt::Value::Undef);
    let p_a = fx.args().get(3).cloned().unwrap_or(rt::Value::Undef);
    let mut parts: Vec<rt::Geo> = Vec::new();
    let l0_blk = {
        let mut parts: Vec<rt::Geo> = Vec::new();
    parts.push(fx.call(&rt::ModuleCall { name: "req_children", args: &[(None, fx.dollar("$children")), ], children: rt::Children::None })?);
    parts.push(fx.call(&rt::ModuleCall { name: "color", args: &[(None, fx.call_fn(&rt::FnCall { name: "hsl", args: &[(None, p_h.clone()), (None, p_s.clone()), (None, p_l.clone())] })?), (None, p_a.clone()), ], children: rt::Children::Compiled(&[&|fx: &dyn rt::ModuleCtx| { let mut parts: Vec<rt::Geo> = Vec::new();     parts.push(fx.children()?);
 Ok(fx.group(parts)) }, ]) })?);
        fx.group(parts)
    };
    parts.push(l0_blk);
    Ok(fx.group(parts))
}


/// Generated native for module `hsv` — geometry through the interpreter's own
/// construction, so a generated module is what interpreting its reference builds.
pub(super) fn hsv(fx: &dyn rt::ModuleCtx) -> rt::Result<rt::Geo> {
    let p_h = fx.args().get(0).cloned().unwrap_or(rt::Value::Undef);
    let p_s = fx.args().get(1).cloned().unwrap_or(rt::Value::Undef);
    let p_v = fx.args().get(2).cloned().unwrap_or(rt::Value::Undef);
    let p_a = fx.args().get(3).cloned().unwrap_or(rt::Value::Undef);
    let mut parts: Vec<rt::Geo> = Vec::new();
    let l0_blk = {
        let mut parts: Vec<rt::Geo> = Vec::new();
    parts.push(fx.call(&rt::ModuleCall { name: "req_children", args: &[(None, fx.dollar("$children")), ], children: rt::Children::None })?);
    parts.push(fx.call(&rt::ModuleCall { name: "color", args: &[(None, fx.call_fn(&rt::FnCall { name: "hsv", args: &[(None, p_h.clone()), (None, p_s.clone()), (None, p_v.clone())] })?), (None, p_a.clone()), ], children: rt::Children::Compiled(&[&|fx: &dyn rt::ModuleCtx| { let mut parts: Vec<rt::Geo> = Vec::new();     parts.push(fx.children()?);
 Ok(fx.group(parts)) }, ]) })?);
        fx.group(parts)
    };
    parts.push(l0_blk);
    Ok(fx.group(parts))
}


/// Generated native for module `rainbow` — geometry through the interpreter's own
/// construction, so a generated module is what interpreting its reference builds.
pub(super) fn rainbow(fx: &dyn rt::ModuleCtx) -> rt::Result<rt::Geo> {
    let p_list = fx.args().get(0).cloned().unwrap_or(rt::Value::Undef);
    let p_stride = fx.args().get(1).cloned().unwrap_or(rt::Value::Undef);
    let p_maxhues = fx.args().get(2).cloned().unwrap_or(rt::Value::Undef);
    let p_shuffle = fx.args().get(3).cloned().unwrap_or(rt::Value::Undef);
    let p_seed = fx.args().get(4).cloned().unwrap_or(rt::Value::Undef);
    let mut parts: Vec<rt::Geo> = Vec::new();
    let l0_ll = fx.call_fn(&rt::FnCall { name: "len", args: &[(None, p_list.clone())] })?;
    let l1_maxhues = fx.call_fn(&rt::FnCall { name: "first_defined", args: &[(None, rt::build_vector(vec![p_maxhues.clone(), l0_ll.clone()]))] })?;
    let l2_huestep = rt::apply_binary(rt::BinOp::Div, rt::Value::Num(f64::from_bits(0x4076800000000000_u64)), l1_maxhues.clone());
    let l5_huelist = { let mut l3_acc: Vec<rt::Value> = Vec::new(); for l4_i in rt::iter_values_native(&rt::build_range(&rt::Value::Num(f64::from_bits(0x0_u64)), &rt::Value::Num(f64::from_bits(0x3ff0000000000000_u64)), &rt::apply_binary(rt::BinOp::Sub, l0_ll.clone(), rt::Value::Num(f64::from_bits(0x3ff0000000000000_u64))))) { l3_acc.push(fx.call_fn(&rt::FnCall { name: "posmod", args: &[(None, rt::apply_binary(rt::BinOp::Add, rt::apply_binary(rt::BinOp::Mul, l4_i.clone(), l2_huestep.clone()), rt::apply_binary(rt::BinOp::Div, rt::apply_binary(rt::BinOp::Mul, l4_i.clone(), rt::Value::Num(f64::from_bits(0x4076800000000000_u64))), p_stride.clone()))), (None, rt::Value::Num(f64::from_bits(0x4076800000000000_u64)))] })?);
        } rt::build_vector(l3_acc) };
    let l6_hues = if p_shuffle.clone().is_truthy() { fx.call_fn(&rt::FnCall { name: "shuffle", args: &[(None, l5_huelist.clone()), (Some("seed"), p_seed.clone())] })? } else { l5_huelist.clone() };
    let l7_blk = {
        let mut parts: Vec<rt::Geo> = Vec::new();
    parts.push(fx.call(&rt::ModuleCall { name: "req_children", args: &[(None, fx.dollar("$children")), ], children: rt::Children::None })?);
    let l8_sd = fx.dollar("$idx");
    for l9_dv in rt::iter_values_native(&fx.call_fn(&rt::FnCall { name: "idx", args: &[(None, p_list.clone())] })?) {
    fx.set_dollar("$idx", l9_dv.clone());
    let l10_sd = fx.dollar("$item");
    fx.set_dollar("$item", rt::index(p_list.clone(), &fx.dollar("$idx")));
    parts.push(fx.call(&rt::ModuleCall { name: "hsv", args: &[(Some("h"), rt::index(l6_hues.clone(), &fx.dollar("$idx"))), ], children: rt::Children::Compiled(&[&|fx: &dyn rt::ModuleCtx| { let mut parts: Vec<rt::Geo> = Vec::new();     parts.push(fx.children()?);
 Ok(fx.group(parts)) }, ]) })?);
    fx.set_dollar("$item", l10_sd);
    }
    fx.set_dollar("$idx", l8_sd);
        fx.group(parts)
    };
    parts.push(l7_blk);
    Ok(fx.group(parts))
}


/// Generated native for module `recolor` — geometry through the interpreter's own
/// construction, so a generated module is what interpreting its reference builds.
pub(super) fn recolor(fx: &dyn rt::ModuleCtx) -> rt::Result<rt::Geo> {
    let p_c = fx.args().get(0).cloned().unwrap_or(rt::Value::Undef);
    let mut parts: Vec<rt::Geo> = Vec::new();
    fx.set_dollar("$color", p_c.clone());
    let l0_blk = {
        let mut parts: Vec<rt::Geo> = Vec::new();
    parts.push(fx.call(&rt::ModuleCall { name: "req_children", args: &[(None, fx.dollar("$children")), ], children: rt::Children::None })?);
    parts.push(fx.children()?);
        fx.group(parts)
    };
    parts.push(l0_blk);
    Ok(fx.group(parts))
}

